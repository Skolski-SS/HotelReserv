<apex:page controller="PrereservToReservPageController">
      <apex:form id="PrereservToReserv">
        <apex:pageBlock title="PreReserv To Reserv">
            <apex:pageMessages id="showmsg"></apex:pageMessages>
            <apex:pageBlockSection columns="1">
                <apex:inputField value="{!prereserv.Hotel__c}" />                 
                <apex:inputField value="{!prereserv.Room__c}"/>
                <apex:inputField value="{!prereserv.ChekinDate__c}"/>
                <apex:inputField value="{!prereserv.ChekoutDate__c}" />
                <apex:inputField value="{!prereserv.Count__c}" />
                <apex:inputField value="{!prereserv.Email__c}" />
                <apex:inputField value="{!room.Alcohol__c}" />
                <apex:inputField value="{!room.Internet__c}" />
                <apex:inputField value="{!room.Sauna__c}" />                
                </apex:pageBlockSection>
            <apex:pageBlockButtons >
                <apex:commandButton value="Reserv Room" action="{!save}"/>
                <apex:commandButton value="Cancel" action="{!cancel}"/>
            </apex:pageBlockButtons>
        </apex:pageBlock>
    </apex:form>
</apex:page>